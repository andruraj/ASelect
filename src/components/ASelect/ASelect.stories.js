import { ASelect } from "./ASelect";

const meta = {
  title: "ASelect",
  component: ASelect,
  argTypes: {
    onChange: {
      action: "changed",
    },
  },
};

export default meta;

export const Default = {};
